"""Noblen Agent Engine (Phase 3).

The reusable runtime that turns the AI Core into governed, versioned agents:
registry, immutable versions, a controlled execution loop, a permissioned tool
registry, scoped memory, conversations, and human-in-the-loop approvals.
"""
